# a Dictionary allows us to group data in collection and assing each item a label
example = {
    1 : 'one',
    2 : 'two',
    3 : 'three',
    4 : 'four',
}

# accessing dictionary elements using the coresponding label
print(example[1])